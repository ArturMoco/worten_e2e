{
  "name": "worten-e2e-tests",
  "version": "1.0.0",
  "scripts": {
  "test:run": "cypress run --headed --browser chrome --config video=true"
}
,
  "devDependencies": {
    "cypress": "^14.0.0",
    "cypress-wait-until": "^3.0.2"
  }
}
